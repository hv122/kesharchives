#include "player.h" // this is used so that the cpp recognises the class names

void Player::Draw() // needs to have the same class "Player" otherwise it doesn't refer to our header file
{
	// the cpp denotes what the functions actually does
}
